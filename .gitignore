# Local tooling & machine-specific config (not part of the site)
.studio-automation/
.claude/
.archive/
.vscode/

# Working files — this repo publishes the site (site/) only
/labs/
/notes/
/*.pdf
/*.rtf
*.excalidraw

# Recordings & cut logs (large local media)
/videos/

# Local ML models (whisper etc.) — huge binaries, never publish
/models/

# Local tooling (cut logger etc.) — studio gear, not site content
/tools/
__pycache__/
*.py[cod]

# Credentials scratch — never again
creds.json

# Personal learning tracker — never publish
Learning Tracker*.csv

# Private schedule calendar (ops-deck) — never publish; work here locally
/calendar/

# OS / editor cruft
.DS_Store
*.swp
*~

# Logs
*.log
